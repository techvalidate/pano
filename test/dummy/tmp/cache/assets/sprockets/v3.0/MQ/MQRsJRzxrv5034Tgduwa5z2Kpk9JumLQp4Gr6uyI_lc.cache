I"%(function() {
  $.isCSSFeatureSupported = function(cssFeature) {
    var cssFeatureUpperCase, domPrefixes, elm, i, j, ref, supported;
    supported = false;
    domPrefixes = 'Webkit Moz ms O'.split(' ');
    elm = document.createElement('div');
    cssFeatureUpperCase = null;
    cssFeature = cssFeature.toLowerCase();
    if (elm.style[cssFeature] !== void 0) {
      supported = true;
    }
    if (supported === false) {
      cssFeatureUpperCase = cssFeature.charAt(0).toUpperCase() + cssFeature.substr(1);
      for (i = j = 0, ref = domPrefixes.length; 0 <= ref ? j < ref : j > ref; i = 0 <= ref ? ++j : --j) {
        if (elm.style[domPrefixes[i] + cssFeatureUpperCase] !== void 0) {
          supported = true;
          break;
        }
      }
    }
    return supported;
  };

}).call(this);
:ET