import { load as onLoad } from '../global/ui'

onLoad(() =>
  // =====================================================
  // color pickers
  // =====================================================

  $('.color-picker-text-field').minicolors()
);
